# Calyx SSH server configuration
# Security hardening: completely disable root login, enforce key-only authentication for normal users
PermitRootLogin no
PubkeyAuthentication yes
PasswordAuthentication no
KbdInteractiveAuthentication no
